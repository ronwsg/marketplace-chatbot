const routes = [
    // {path: '/realpath', getComponent: get<NAME>Component, name: '<NAME>'},
]

export default routes

// function get<NAME>Component(nextState, cb) {
//     require.ensure([], (require) => {
//         cb(null, require('./components/<NAME>').default)
//     }, '<NAME>')
// }
